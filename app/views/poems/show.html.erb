<div class="container">
    <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
        <%= form_for(@poem) do |f| %>
            <p>タイトル：<%= @poem.title %></p>
            <p>本文：<%= @poem.content %></p>
        <% end %>
        <%= link_to "ポエム一覧画面に戻る", poems_path %>
    </div>
</div>
